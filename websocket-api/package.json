{
  "name": "websocket-api",
  "version": "0.0.0",
  "description": "API typedefs for WebSocket communication between server and client (shared between both)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "build": "rm -r dist && tsc"
  },
  "author": "Samo Kolter",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "socket.io": "^4.6.2",
    "socket.io-client": "^4.6.1"
  }
}
